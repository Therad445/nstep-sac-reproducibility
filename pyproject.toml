[project]
name = "nstep-sac-reproducibility"
version = "0.1.0"
description = "Small-compute reproduction and ablation study of n-step Soft Actor-Critic variants"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
pythonpath = ["."]
